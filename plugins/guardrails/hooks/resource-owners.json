{
  "$schema_version": 2,
  "_note": "No resources are governed by default. Add your own in ~/.claude/guardrails.resources.json, which replaces this file rather than merging with it. Until 0.5.0 this shipped two directories from the author's own machine, protected by an `owners` rule that no longer exists; see the plugin README for what replaced it and why.",
  "resources": []
}
