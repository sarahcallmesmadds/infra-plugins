{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "[ -e \"${CLAUDE_PLUGIN_ROOT}/bin/hook-node\" ] || { echo \"Plugin hooks are off in this session because a plugin was updated after it started. Restart to switch them back on.\" >&2; exit 3; }; [ -x \"${CLAUDE_PLUGIN_ROOT}/bin/hook-node\" ] || { echo \"Plugin hooks are off because ${CLAUDE_PLUGIN_ROOT}/bin/hook-node is not executable. A restart will not help. Restore its execute bit with chmod +x.\" >&2; exit 3; }; \"${CLAUDE_PLUGIN_ROOT}\"/bin/hook-node \"${CLAUDE_PLUGIN_ROOT}\"/hooks/skill-md-check.js"
          },
          {
            "type": "command",
            "command": "[ -e \"${CLAUDE_PLUGIN_ROOT}/bin/hook-node\" ] || { echo \"Plugin hooks are off in this session because a plugin was updated after it started. Restart to switch them back on.\" >&2; exit 3; }; [ -x \"${CLAUDE_PLUGIN_ROOT}/bin/hook-node\" ] || { echo \"Plugin hooks are off because ${CLAUDE_PLUGIN_ROOT}/bin/hook-node is not executable. A restart will not help. Restore its execute bit with chmod +x.\" >&2; exit 3; }; \"${CLAUDE_PLUGIN_ROOT}\"/bin/hook-node \"${CLAUDE_PLUGIN_ROOT}\"/hooks/deps-watch.js"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "[ -e \"${CLAUDE_PLUGIN_ROOT}/bin/hook-node\" ] || { echo \"Plugin hooks are off in this session because a plugin was updated after it started. Restart to switch them back on.\" >&2; exit 3; }; [ -x \"${CLAUDE_PLUGIN_ROOT}/bin/hook-node\" ] || { echo \"Plugin hooks are off because ${CLAUDE_PLUGIN_ROOT}/bin/hook-node is not executable. A restart will not help. Restore its execute bit with chmod +x.\" >&2; exit 3; }; \"${CLAUDE_PLUGIN_ROOT}\"/bin/hook-node \"${CLAUDE_PLUGIN_ROOT}\"/hooks/notice-correction.js"
          },
          {
            "type": "command",
            "command": "[ -e \"${CLAUDE_PLUGIN_ROOT}/hooks/hook-health-probe.sh\" ] || { echo \"Plugin hooks are off in this session because a plugin was updated after it started. Restart to switch them back on.\" >&2; exit 3; }; [ -x \"${CLAUDE_PLUGIN_ROOT}/hooks/hook-health-probe.sh\" ] || { echo \"Plugin hooks are off because ${CLAUDE_PLUGIN_ROOT}/hooks/hook-health-probe.sh is not executable. A restart will not help. Restore its execute bit with chmod +x.\" >&2; exit 3; }; \"${CLAUDE_PLUGIN_ROOT}\"/hooks/hook-health-probe.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
