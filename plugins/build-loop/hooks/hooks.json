{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "[ -n \"${CLAUDE_PLUGIN_ROOT}\" ] || { echo \"Plugin hooks are off because this host did not tell the hook where the plugin is installed. A restart will not help.\" >&2; exit 3; }; [ -e \"${CLAUDE_PLUGIN_ROOT}/bin/hook-node\" ] || { echo \"Plugin hooks are off in this session because a plugin was updated after it started. Restart to switch them back on.\" >&2; exit 3; }; [ -x \"${CLAUDE_PLUGIN_ROOT}/bin/hook-node\" ] || { echo \"Plugin hooks are off because ${CLAUDE_PLUGIN_ROOT}/bin/hook-node is not executable. A restart will not help. Restore its execute bit with chmod +x.\" >&2; exit 3; }; \"${CLAUDE_PLUGIN_ROOT}\"/bin/hook-node \"${CLAUDE_PLUGIN_ROOT}\"/hooks/skill-md-check.js"
          },
          {
            "type": "command",
            "command": "[ -n \"${CLAUDE_PLUGIN_ROOT}\" ] || { echo \"Plugin hooks are off because this host did not tell the hook where the plugin is installed. A restart will not help.\" >&2; exit 3; }; [ -e \"${CLAUDE_PLUGIN_ROOT}/bin/hook-node\" ] || { echo \"Plugin hooks are off in this session because a plugin was updated after it started. Restart to switch them back on.\" >&2; exit 3; }; [ -x \"${CLAUDE_PLUGIN_ROOT}/bin/hook-node\" ] || { echo \"Plugin hooks are off because ${CLAUDE_PLUGIN_ROOT}/bin/hook-node is not executable. A restart will not help. Restore its execute bit with chmod +x.\" >&2; exit 3; }; \"${CLAUDE_PLUGIN_ROOT}\"/bin/hook-node \"${CLAUDE_PLUGIN_ROOT}\"/hooks/deps-watch.js"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "say(){ if [ -n \"${PLUGIN_ROOT}\" ] && [ \"${PLUGIN_ROOT}\" = \"${CLAUDE_PLUGIN_ROOT}\" ]; then printf '{\"hookSpecificOutput\":{\"hookEventName\":\"UserPromptSubmit\",\"additionalContext\":\"%s\"}}' \"${2:-$1}\"; exit 0; fi; printf '%s\\n' \"$1\" >&2; exit 3; }; [ -n \"${CLAUDE_PLUGIN_ROOT}\" ] || say \"Plugin hooks are off because this host did not tell the hook where the plugin is installed. A restart will not help.\"; [ -e \"${CLAUDE_PLUGIN_ROOT}/bin/hook-node\" ] || say \"Plugin hooks are off in this session because a plugin was updated after it started. Restart to switch them back on.\"; [ -x \"${CLAUDE_PLUGIN_ROOT}/bin/hook-node\" ] || say \"Plugin hooks are off because ${CLAUDE_PLUGIN_ROOT}/bin/hook-node is not executable. A restart will not help. Restore its execute bit with chmod +x.\" \"Plugin hooks are off because bin/hook-node inside the plugin directory is not executable. A restart will not help. Restore its execute bit with chmod +x.\"; \"${CLAUDE_PLUGIN_ROOT}\"/bin/hook-node \"${CLAUDE_PLUGIN_ROOT}\"/hooks/notice-correction.js"
          },
          {
            "type": "command",
            "command": "say(){ if [ -n \"${PLUGIN_ROOT}\" ] && [ \"${PLUGIN_ROOT}\" = \"${CLAUDE_PLUGIN_ROOT}\" ]; then printf '{\"hookSpecificOutput\":{\"hookEventName\":\"UserPromptSubmit\",\"additionalContext\":\"%s\"}}' \"${2:-$1}\"; exit 0; fi; printf '%s\\n' \"$1\" >&2; exit 3; }; [ -n \"${CLAUDE_PLUGIN_ROOT}\" ] || say \"Plugin hooks are off because this host did not tell the hook where the plugin is installed. A restart will not help.\"; [ -e \"${CLAUDE_PLUGIN_ROOT}/hooks/hook-health-probe.sh\" ] || say \"Plugin hooks are off in this session because a plugin was updated after it started. Restart to switch them back on.\"; [ -x \"${CLAUDE_PLUGIN_ROOT}/hooks/hook-health-probe.sh\" ] || say \"Plugin hooks are off because ${CLAUDE_PLUGIN_ROOT}/hooks/hook-health-probe.sh is not executable. A restart will not help. Restore its execute bit with chmod +x.\" \"Plugin hooks are off because hooks/hook-health-probe.sh inside the plugin directory is not executable. A restart will not help. Restore its execute bit with chmod +x.\"; \"${CLAUDE_PLUGIN_ROOT}\"/hooks/hook-health-probe.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
