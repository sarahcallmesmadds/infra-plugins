# Skill-level UI metadata generated by the official skill scaffolder. The
# plugin-level interface in .codex-plugin/plugin.json describes Build Loop as a
# whole; this file describes only address-devin-review.
interface:
  display_name: "Address Devin Review"
  short_description: "Resolve a full Devin review without point fixes"
  default_prompt: "Use $address-devin-review to classify and resolve every finding in the latest Devin review round."
